/**
 * One height for every control in the app.
 *
 * Tree rows, the accent swatch and the segmented toggles already stood at
 * 32px; inputs and buttons did not. Pinning them all to the same token keeps
 * the whole UI on one rhythm and leaves a single place to change it.
 */
export const CONTROL_HEIGHT = "h-8";
